---
layout: post
title: "[Vue] 뷰 기본 문법 익히기"
date: 2025-10-01
categories: Vue
---

# #뷰 애플리케이션의 기본 구조

## [package.json]

> 기본 정보, 의존성 모듈 정보, 스크립트 명령어 정보 등을 담고 있음

```json
{
  "name": "npm-vue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.7",
    "vite-plugin-vue-devtools": "^8.0.2"
  }
}
```

- `name` : 뷰 애플리케이션의 이름을 나타내는 문자열
- `version` : 뷰 애플리케이션의 버전을 나타내는 문자열
  - "0.0.0"은 [major],[minor],[patch] 버전을 의미
  - **major** : 하위 호환성이 없는 변경 사항이 있을 때 증가
  - **minor** : 하위 호환성이 있는 기능을 추가하거나 변경 사항이 있을 때 증가
  - **patch** : 버그나 작은 수정사항 있을 때 증가
- `private` : 뷰 애플리케이션의 공개 여부를 의미하는 논리형 값
  - true -> 비공개(공개 저장소에 배포하지 않는다는 뜻)
- `scripts` : 뷰 애플리케이션을 빌드하거나 실행할 수 있는 명령어를 등록하는 부분, 객체 형태로 작성, `npm run` 명령어로 실행할 수 있음
- `dependencies` : 뷰 애플리케이션을 실행할 때 필요한 의존성 모듈을 정의하는 부분
- `devDependencies` : 뷰 애플리케이션을 개발할 때 필요한 의존성 모듈을 정의하는 부분

```bash
npm install [패키지명] [--save | --save-dev]
```

- `--save` 옵션 -> dependencies에 설치
- `--save-dev` 옵션 -> devDependencies에 설치

## [index.html]

> 개발 서버가 구동되면 가장 먼저 서버에 로드

```html
<!DOCTYPE html>
<html lang="">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vite App</title>
  </head>
  <body>
    <!-- 1 -->
    <div id="app"></div>
    <!-- 2 -->
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

1. id 속성의 값이 "app"인 HTML 요소를 루트 요소로 지정
2. main.js 모듈을 불러오는 부분

## [main.js]

> 뷰 애플리케이션을 초기화하고 구성하는 역할

```javascript
import "./assets/main.css"; // 1

import { createApp } from "vue"; // 2
import App from "./App.vue"; // 3

createApp(App).mount("#app"); // 4
```

1. 컴포넌트에 스타일 적용
2. vue 패키지에서 함수 가져옴
3. App.vue 파일 불러옴 -> 루트 컴포넌트
4. 뷰 애플리케이션의 인스턴스 생성

## [App.vue]

> **루트 컴포넌트** : 뷰 애플리케이션이 최초로 진입하는 가장 상위에 있는 컴포넌트

---

# #뷰 애플리케이션 인스턴스

## [인스턴스]

> 클래스의 실체화된 객체를 의미, 특정 클래스의 상태를 나타냄

- **클래스**가 객체를 생성하기 위한 템플릿이라면, **인스턴스**는 해당 클래스의 실제 객체

```javascript
import "./assets/main.css";

import { createApp } from "vue"; // 1
import App from "./App.vue";

createApp(App).mount("#app"); // 2
```

1. `createApp()` 함수는 뷰 패키지에 정의된 Vue 클래스의 인스턴스를 생성하는 역할을 하는 함수
2. `createApp()` 함수로 Vue 인스턴스 생성하고, 생성한 인스턴스를 `mount()` 함수로 HTML DOM과 연결

## [인스턴스의 구성 요소]

```javascript
// main.js
import { createApp, h } from "vue";

createApp({
  data() {
    return {
      message: "Hello, Vue!",
    };
  },
  methods: {
    reverse() {
      this.message = this.message.split("").reverse().join("");
    },
  },
  render() {
    // data와 methods에 정의한 속성 사용
    return h("div", [
      h("p", this.message),
      h("button", { onClick: this.reverse }, "Reverse"),
    ]);
  },
}).mount("#app");
```

- **인스턴스의 구성 요소** : `data`, `methods`, `render`와 같은 속성들

## [SFC의 구조]

> **단일 구성 요소** : 하나의 파일에 인스턴스 구성 요소와 관련한 모든 코드가 포함되는 형식

```html
<!-- App.vue -->
<script>
  export default {};
</script>
<template></template>
<style></style>
```

### 1. \<script> 태그 영역

- SFC 파일에서 사용할 로직을 자바스크립트로 작성
- **옵션스 API** : 뷰에서 객체를 작성할 때 지켜야 하는 문법 규칙

### 2. \<template> 태그 영역

- HTML 코드 작성
- \<script> 태그 영역에서 작성한 여러 속성으로 정의한 값들과 연동할 수 있음

### 3. \<style> 태그 영역

- CSS 스타일을 적용하기 위해 사용

---

# #기본 문법 다루기

## [옵션스 API 사용하기]

> 뷰에서 약속된 속성을 사용해야 함

| 속성               | 설명                                                    |
| ------------------ | ------------------------------------------------------- |
| `data`             | 데이터(data)를 저장하는 속성                            |
| `methods`          | 함수(function)를 저장하는 속성                          |
| `computed`         | 데이터에 기반해 계산된 속성                             |
| `watch`            | 데이터의 변화를 감시하기 위한 속성                      |
| `props`            | 사용자 정의 데이터를 수신하기 위한 속성                 |
| `emits`            | 발신 가능한 부모 컴포넌트의 이벤트를 정의하기 위한 속성 |
| `provide`          | 하위 컴포넌트에 데이터를 주입하기 위한 속성             |
| `inject`           | 상위 컴포넌트에 주입된 데이터를 받기 위한 속성          |
| `Life Cycle Hooks` | 컴포넌트의 특정 시점에 따라 자동으로 호출되는 콜백 함수 |

## [데이터 정의하기]

```html
<!-- App.vue -->
<script>
  export default {
    data() {
      return {
        // 상황과 문맥에 따라 '변수', '데이터', '데이터 속성'이라고 함
        name: "철수",
        age: 19,
      };
    },
  };
</script>
```

## [데이터 보간 사용하기]

> **텍스트 보간** : 콧수염처럼 생긴 중괄호 두 쌍으로 표시하는 콧수염 문법으로 구성 `{{ 데이터 속성 }}`

```html
<!-- App.vue -->
<template>
  <p>내 이름은 {{ name }}입니다.</p>
  <p>나이는 {{ age }}살입니다.</p>

  {{ 10 * 20 * 30 }} {{ name.toUpperCase() }}
</template>
```

## [디렉티브 사용하기]

> 뷰에서만 사용하는 문법, 접두사 'v-'가 붙음

### 1. v-html

- 값에 HTML 태그가 포함되었으면 값을 단순 텍스트로 치환해 출력
- ❗️ XSS 공격에 매우 취약하므로 사용자가 입력한 데이터를 출력할 때는 사용 불가, 개발자가 직접 작성한 데이터를 출력할 때만 사용해야 함 ❗️

```html
<!-- App.vue/v-html -->
<script>
  export default {
    data() {
      return {
        message: "<h1>Hello, Vue JS!</h1>",
      };
    },
  };
</script>

<template>
  <div v-html="message"></div>
</template>

<!-- 결과 -->
<!-- Hello, Vue JS! -->
```

### 2. v-text

- 값을 단순하게 텍스트로 출력하게 하는 디렉티브

```html
<!-- App.vue/v-text -->
<template>
  <div v-text="message"></div>
</template>

<!-- 결과 -->
<!-- <h1>Hello, Vue JS!</h1> -->
```

### 3. v-pre

- 컴파일할 때 `<template>` 태그 영역을 건너뛰게 하는 디렉티브
- 컴파일에서 제외되어 단순하게 텍스트로 취급되어 그대로 출력

```html
<!-- App.vue/v-pre -->
<script>
  export default {
    data() {
      return {
        message: "Hello, Vue JS!",
      };
    },
  };
</script>

<template>
  <div v-pre>{{ message }}</div>
</template>

<!-- 결과 -->
<!-- {{ message }} -->
```

### 4. v-bind

- HTML 태그의 속성 앞에 콜론을 붙여 표시하며 속성과 데이터를 연결할 수 있음
- 현업에서는 축약형을 많이 사용함

```html
<!-- App.vue/v-bind -->
<script>
  export default {
    data() {
      return {
        className: "red-color",
      };
    },
  };
</script>

<template>
  <h1 v-bind:class="className">Hello, Vue JS</h1>
  <!-- 축약형 -->
  <h1 :class="className">Hello, Vue JS</h1>
</template>

<style>
  .red-color {
    color: red;
  }
</style>
```

### 5. v-if/v-else-if/v-else

- `<template>` 태그 영역에서 조건부 렌더링 기능을 구현하는 디렉티브
- **조건부 렌더링** : 디렉티브에 할당된 값의 참/거짓에 따라 HTML 요소를 렌더링하거나 렌더링하지 않는 것을 의미
- 거짓일 때 `v-if` 부분이 주석 처리되어 요소가 DOM에 추가되지 않음

```html
<!-- App.vue/v-if -->
<script>
  export default {
    data() {
      return {
        condition: "A",
      };
    },
  };
</script>

<template>
  <p v-if="condition === 'A'">condition 데이터의 값은 A입니다.</p>
  <p v-else-if="condition === 'B'">condition 데이터의 값은 B입니다.</p>
  <p v-else-if="condition === 'C'">condition 데이터의 값은 C입니다.</p>
  <p v-else-if="condition === 'D'">condition 데이터의 값은 D입니다.</p>
  <p v-else>어떤 조건에도 해당하지 않습니다.</p>
</template>

<!-- 결과 -->
<!-- condition 데이터의 값은 A입니다. -->
```

### 6. v-show

- 디렉티브의 값의 참/거짓에 따라 `<template>` 태그 영역에 작성된 요소를 숨기거나 보여줌
- 거짓일 때 CSS의 `display` 속성으로 감춤

```html
<!-- App.vue/v-show -->
<script>
  export default {
    data() {
      return {
        condition: true,
      };
    },
  };
</script>

<template>
  <p v-show="condition">v-show 디렉티브는 조건이 참입니다.</p>
  <p v-show="!condition">v-show 디렉티브는 조건이 거짓입니다.</p>
</template>

<!-- 결과 -->
<!-- v-show 디렉티브는 조건이 참입니다. -->
```

#### 💡 v-if/v-show

- 조건이 자주 변경되는 경우 -> `v-show`
- 조건이 자주 변경되지 않는 경우 -> `v-if`

### 7. v-cloak

- 뷰 애플리케이션의 렌더링 과정에서 데이터 바인딩이 완료되기 전에 데이터가 화면에 노출되는 것을 막기 위해 사용

```html
<!-- cdn test -->
<!-- index.html/v-cloak -->
<head>
  <style>
    [v-cloak] {
      display: none;
    }
  </style>
</head>
<body>
  <div id="app">
    <h1 v-cloak>{{ message }}</h1>
  </div>
  <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
  <script>
    const { createApp } = Vue;
    window.setTimeout(() => {
      createApp({
        data() {
          return {
            message: "Hello Vue!",
          };
        },
      }).mount("#app");
    }, 2000);
  </script>
</body>
```

### 8. v-for

- 뷰에서 반복적으로 렌더링하는 HTML 요소를 생성하는 데 사용

```javascript
v-for="값 in 배열" key="고유값"
v-for="(값, 인덱스) in 배열" key="고유값"
```

```html
<!-- App.vue/v-for -->
<script>
  export default {
    data() {
      return {
        fruits: ["apple", "banana", "orange"],
      };
    },
  };
</script>

<template>
  <h1>fruits 데이터의 반복 결과</h1>
  <ul>
    <li v-for="(fruit, index) in fruits" :key="index">
      인덱스: {{ index }}, 값: {{ fruit }}
    </li>
  </ul>
</template>

<!-- 결과 -->
<!-- 인덱스: 0, 값: apple
인덱스: 1, 값: banana
인덱스: 2, 값: orange -->
```

- 객체가 할당된 데이터 속성의 값도 `v-for` 디렉티브로 반복 가능

```javascript
v-for="값 in 객체" key="고유값"
v-for="(값, 키) in 객체" key="고유값"
v-for="(값, 키, 인덱스) in 객체" key="고유값"
```

```html
<!-- App.vue/v-for -->
<script>
  export default {
    data() {
      return {
        user: {
          name: "John",
          age: 20,
          gender: "male",
        },
      };
    },
  };
</script>

<template>
  <h1>user 데이터의 반복 결과</h1>
  <ul>
    <li v-for="(value, key, index) in user" :key="index">
      {{ index }}: {{ key }} : {{ value }}
    </li>
  </ul>
</template>

<!-- 결과 -->
<!-- 0: name : John
1: age : 20
2: gender : male -->
```

---

# #이벤트 다루기

> **이벤트(event)** : 사용자와 상호작용해서 발생하는 일련의 사건

## [v-on 디렉티브와 methods 옵션 속성으로 이벤트 연결하기]

- `v-on` 디렉티브로 이벤트 연결
  - `v-on:이벤트_타입="이벤트_핸들러"`

1. 이벤트 타입

   - 예) `click`, `keyup`, `keydown` 등
   - 이벤트 종류를 식별하는 문자열로 자바스크립트와 개념이 같음

2. 이벤트 핸들러

   - 이벤트 타입과 일치하는 이벤트가 발생하면 실행되는 함수
   - ```javascript
     export default {
       methods: {}, // 메서드 정의
     };
     ```

📌 [자바스크립트의 이벤트 타입](https://developer.mozilla.org/en-US/docs/Web/API/Element)

```html
<!-- App.vue/v-on -->
<script>
  export default {
    methods: {
      // ES6의 단축 메서드명으로 표기
      clickHandler() {
        alert("click!");
      },
    },
  };
</script>

<template>
  <button type="button" v-on:click="clickHandler">클릭</button>
  <button type="button" v-on:dblclick="clickHandler">더블클릭</button>
  <!-- 축약형 -->
  <button type="button" @dblclick="clickHandler">더블클릭</button>
</template>
```

## [이벤트 객체 사용하기]

> **이벤트 객체** : 특정 이벤트에 대한 상세 정보를 가지고 있는 객체

```html
<!-- App.vue/event -->
<script>
  export default {
    methods: {
      clickHandler(event, message) {
        console.log(event);
      },
    },
  };
</script>

<template>
  <button type="button" @click="clickHandler($event)">클릭</button>
  <!-- 화살표 함수 사용 -->
  <button type="button" @click="($event) => clickHandler($event)">클릭</button>
  <!-- 이벤트 객체와 값을 함께 사용 -->
  <button type="button" @click="clickHandler($event, 'Hello')">클릭</button>
</template>
```

## [수식어 사용하기]

> **수식어(modifier)** : 이벤트 처리 방식을 제어하는 데 사용하는 기능

- 일반적인 DOM 이벤트 동작을 변경하거나 기능을 추가할 수 있음
- `@이벤트_타입.수식어`

```html
<!-- App.vue/modifier -->
<script>
  export default {
    methods: {
      // onkeyupHandler(e) {
      //   // 키보드 이벤트 객체에서 keyCode 속성 13은 Enter 키
      //   if (e.keyCode === 13) {
      //     console.log("Enter!");
      //   }
      // },
      onkeyupHandler() {
        console.log("Enter!");
      },
    },
  };
</script>

<template>
  <!-- <input type="text" @keyup="($event) => onkeyupHandler($event)" /> -->
  <!-- 수식어 사용 -->
  <input type="text" @keyup.enter="onkeyupHandler" />
</template>
```

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>수식어</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td rowspan="6">이벤트 수식어</td>
      <td><code class="language-plaintext highlighter-rouge">.stop</code></td>
      <td>이벤트 전파를 상위 요소로 전달하지 않음(이벤트 버블링 금지)</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.prevent</code></td>
      <td>기본 이벤트 제거</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.self</code></td>
      <td>이벤트가 발생한 요소 자체에서만 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.capture</code></td>
      <td>이벤트 캡처링 활성화</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.once</code></td>
      <td>이벤트 발생을 한 번으로 제한</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.passive</code></td>
      <td>스크롤 동작을 기다리지 않고 즉시 처리</td>
    </tr> 
    <tr>
      <td rowspan="9">키보드 입력 키 수식어</td>
      <td><code class="language-plaintext highlighter-rouge">.enter</code></td>
      <td>Enter 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.tab</code></td>
      <td>Tab 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.delete</code></td>
      <td>Del 키나 backspace 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.esc</code></td>
      <td>ESC 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.space</code></td>
      <td>space 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.up</code></td>
      <td>↑ 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.down</code></td>
      <td>↓ 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.left</code></td>
      <td>← 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.right</code></td>
      <td>→ 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td rowspan="4">시스템 입력 키 수식어</td>
      <td><code class="language-plaintext highlighter-rouge">.ctrl</code></td>
      <td>Ctrl 키를 눌렀을 때 이벤트 처리</td>
    </tr>  
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.alt</code></td>
      <td>Alt 키를 눌렀을 때 이벤트 처리</td>
    </tr> 
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.shift</code></td>
      <td>Shift 키를 눌렀을 때 이벤트 처리</td>
    </tr> 
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.meta</code></td>
      <td>메타 키(Win/command)를 눌렀을 때 이벤트 처리</td>
    </tr> 
    <tr>
      <td>특별 수식어</td>
      <td><code class="language-plaintext highlighter-rouge">.exact</code></td>
      <td>키 이벤트 처리 시 특정 키 조합만을 처리하는 데 사용</td>
    </tr>  
    <tr>
      <td rowspan="3">마우스 버튼 수식어</td>
      <td><code class="language-plaintext highlighter-rouge">.left</code></td>
      <td>마우스 왼쪽 버튼을 클릭했을 때 이벤트 처리</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.right</code></td>
      <td>마우스 오른쪽 버튼을 클릭했을 때 이벤트 처리</td>
    </tr> 
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.middle</code></td>
      <td>마우스 가운데(휠) 버튼을 클릭했을 때 이벤트 처리</td>
    </tr> 
  </tbody>
</table>

📌 [수식어에 대한 자세한 설명](https://ko.vuejs.org/guide/essentials/event-handling)

---

# #폼 다루기

---

# #계산된 속성과 감시자 속성

---

# #스타일 다루기
